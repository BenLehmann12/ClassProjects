import unittest
from intro_to_functions import camper_age_input

class MyTestCase(unittest.TestCase):
    def test_something(self):
        self.assertEqual(3, camper_age_input.convert_to_months(3))

if __name__ == '__main__':
    unittest.main()

    def convert_to_months(years):
        months = convert_to_months(years)
        return months
    if __name__ == '__main__':
        print("3 years is" + convert_to_months(years=3))
